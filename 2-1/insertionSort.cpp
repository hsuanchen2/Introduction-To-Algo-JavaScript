#include <iostream>

void insertionSort(int arr[], int n)
{
    for (int i = 0; i < n; i++)
    {
        const double currentValue = arr[i];
        int j = i - 1;
        while (arr[j] > currentValue & j >= 0)
        {
        }
    }
}
int main()
{
}